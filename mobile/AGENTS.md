# IIAS Flutter agent notes (compressed)

App in `lib/`. Riverpod + go_router + Dio → Laravel `/api/v1`.

See `../.cursor/rules/flutter.mdc` + `../.cursor/rules/token-saving.mdc`.
Root `../README.md` for `API_BASE_URL` / device URLs.
